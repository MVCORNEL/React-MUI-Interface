import { Typography } from '@mui/material';
import { Form } from 'react-router-dom';
const AskUs = () => {
    return (
        <div>
            <Typography variant={'h4'} component="h1" mb={6}>
                Chat service not implemented yet.
            </Typography>
        </div>
    );
};

export default AskUs;
